package qms.product.application;

import qms.product.Product;

public class Application extends Product {
    public Application() {
        type = "Application";
        price = 300;
        description = "HTML5 based display controller interface";
    }
}
